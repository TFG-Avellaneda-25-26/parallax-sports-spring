$kind: http-request
name: Sync Year
method: POST
url: '{{baseUrl}}/api/admin/formula1/sync/:year'
order: 1000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
pathVariables:
  - key: year
    value: '{{formula1Year}}'
    description: Year to sync F1 data for
